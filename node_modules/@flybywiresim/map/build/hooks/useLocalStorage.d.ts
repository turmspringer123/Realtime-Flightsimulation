import { Dispatch, SetStateAction } from 'react';
declare type UseLocalStorageOptions<T> = Partial<{
    valueOverride: boolean | T | undefined;
}>;
declare function useLocalStorage<T>(key: string, initialValue: T, options: UseLocalStorageOptions<T>): [T, Dispatch<SetStateAction<T>>];
export default useLocalStorage;
